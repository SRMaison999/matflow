import { Calendar } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function ReservationsPage() {
  return (
    <PlaceholderPage
      title="Reservations"
      description="Gestion des reservations de materiel."
      icon={Calendar}
    />
  );
}
