import { PackageX } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function ReturnsPage() {
  return (
    <PlaceholderPage
      title="Retours"
      description="Gestion des retours et controle du materiel."
      icon={PackageX}
    />
  );
}
