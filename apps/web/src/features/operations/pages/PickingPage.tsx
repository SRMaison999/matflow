import { PackageCheck } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function PickingPage() {
  return (
    <PlaceholderPage
      title="Picking"
      description="Preparation des commandes et listes de picking."
      icon={PackageCheck}
    />
  );
}
