import { ClipboardList } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function OperationsPage() {
  return (
    <PlaceholderPage
      title="Operations"
      description="Vue d'ensemble des operations de picking et retours."
      icon={ClipboardList}
    />
  );
}
