import { Receipt } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function BillingPage() {
  return (
    <PlaceholderPage
      title="Facturation"
      description="Vue d'ensemble de la facturation et finances."
      icon={Receipt}
    />
  );
}
