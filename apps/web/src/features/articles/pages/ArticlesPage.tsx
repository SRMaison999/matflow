import { Package } from 'lucide-react';
import { PlaceholderPage } from '@/components/ui';

export default function ArticlesPage() {
  return (
    <PlaceholderPage
      title="Articles"
      description="Gestion du catalogue d'articles et equipements."
      icon={Package}
    />
  );
}
